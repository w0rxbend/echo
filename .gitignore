# Build output
/bin/
/matrix-proxy
*.exe

# Auto-generated Swagger 2.0 spec — regenerate with: go generate ./cmd/matrix-proxy
# Committed to repo so the binary embeds it; rebuilt in CI.
# internal/integrations/httpapi/swaggerdocs/

# Local environment — contains secrets; use .env.example as template
.env

# Local config — contains host IPs and tokens; use configs/config.example.yaml as template
configs/config.yaml
configs/animations.yaml
configs/rules.yaml

# Docker volumes (if ever bind-mounted locally)
data/

# Editor and OS artefacts
.DS_Store
.idea/
.vscode/
*.swp
*.swo
